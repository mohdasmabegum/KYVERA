version: '3.8'

services:
  kyvera-app:
    build:
      context: ..
      dockerfile: backend/Dockerfile
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
    volumes:
      - kyvera_data:/app/backend
    restart: always

volumes:
  kyvera_data:
